'use client'

import { FormWrapperResetPassword } from '@/features/user/authenticate/components/form-wrapper-reset-password'

export default function ResetPasswordPage() {
  return (
    <section>
      <FormWrapperResetPassword />
    </section>
  )
}
